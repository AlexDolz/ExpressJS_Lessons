<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css" />
    <title>Document</title>
  </head>
  <body>
    <%- include('blocks/header.ejs') -%>

    <h1><%= title %></h1>
    <% if (username !== 'steven'){ %>
    <h2><%= username %></h2>
    <% } else {%>
    <p>Languages of Steven</p>
    <ul>
      <% languages.forEach(elem => { %>
      <li><%= elem %></li>
      <%})%>
    </ul>
    <% } %>
  </body>
</html>
